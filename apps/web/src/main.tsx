import React,{useEffect,useMemo,useRef,useState}from'react';
import{createRoot}from'react-dom/client';
import{Activity,AlertTriangle,BarChart3,Bot,Building2,CheckCircle2,ChevronRight,CircleDollarSign,ClipboardList,FileCheck2,Gauge,Home,KeyRound,Languages,LockKeyhole,LogOut,MapPinned,Mic,Search,Settings,ShieldCheck,Sparkles,Users,VolumeX,X}from'lucide-react';
import'./styles.css';

type Project={id:string;name:string;province:string;municipality:string;contractor:string;programme:string;status:string;healthScore:number;confidence:number;budget:number;expenditure:number;physicalProgress:number;plannedProgress:number;unitsPlanned:number;unitsCompleted:number;overdueMilestones:number;openRisks:number;evidenceAgeDays:number;forecastCompletion:string;primaryBlocker:string;trend:string;rootCauses:string[];projectManager:string;implementingAgent:string;ward:string;stage:string;startDate:string;recoveryActions:any[];activity:any[]};
type Dashboard={totalProjects:number;critical:number;atRisk:number;watch:number;onTrack:number;completed:number;unitsPlanned:number;unitsCompleted:number;budget:number;expenditure:number;overdueMilestones:number;openRisks:number;averagePhysicalProgress:number;highDivergenceProjects:number};
type User={id:string;name:string;email:string;role:string;province?:string;organization?:string;permissions:string[]};
type View='command'|'projects'|'map'|'contractors'|'finance'|'risks'|'recovery'|'audit'|'usage';
type ChatMsg={role:'user'|'assistant';text:string};
type Model='gpt-5.6-luna'|'gpt-5.6-terra'|'gpt-5.6-sol';
const API=import.meta.env.VITE_API_URL||(import.meta.env.PROD?'':'http://localhost:8080');
const currency=(n:number)=>new Intl.NumberFormat('en-ZA',{style:'currency',currency:'ZAR',maximumFractionDigits:0}).format(n);
const statusClass=(s:string)=>s.toLowerCase().replaceAll(' ','-');
const voiceLanguages=[['en-ZA','English (South Africa)'],['af-ZA','Afrikaans'],['zu-ZA','isiZulu'],['xh-ZA','isiXhosa'],['st-ZA','Sesotho'],['tn-ZA','Setswana'],['nso-ZA','Sepedi'],['ts-ZA','itsonga'],['ve-ZA','Tshivenda'],['ss-ZA','siSwati'],['nr-ZA','isiNdebele']];
const demoAccounts=[['admin@homeflow.ai','Administrator'],['executive@homeflow.ai','National Executive'],['manager@homeflow.ai','Gauteng Programme Manager'],['inspector@homeflow.ai','Site Inspector'],['contractor@homeflow.ai','Contractor']];
const navItems:Array<[View,string,any]>=[['command','Command Centre',Gauge],['projects','Projects',Building2],['map','Delivery Map',MapPinned],['contractors','Contractors',Users],['finance','Finance',CircleDollarSign],['risks','Risks & Blockers',AlertTriangle],['recovery','Recovery Plans',Activity],['audit','Audit & Governance',ShieldCheck],['usage','Usage Logs',ClipboardList]];
const roleViews:Record<string,View[]>={Administrator:['command','projects','map','contractors','finance','risks','recovery','audit','usage'],'National Executive':['command','projects','map','contractors','finance','risks','recovery','audit','usage'],'Provincial Programme Manager':['command','projects','map','contractors','finance','risks','recovery'],'Site Inspector':['command','projects','map','risks'],'Contractor':['command','projects','recovery']};
function Metric({label,value,icon:Icon,tone='blue'}:{label:string;value:React.ReactNode;icon:any;tone?:string}){return <div className="metric"><div className={`metric-icon ${tone}`}><Icon size={19}/></div><div><small>{label}</small><strong>{value}</strong></div></div>}
function Status({value}:{value:string}){return <span className={`status ${statusClass(value)}`}>{value}</span>}
function Login({onLogin}:{onLogin:(token:string,user:User)=>void}){const[email,setEmail]=useState('admin@homeflow.ai');const[password,setPassword]=useState('HomeFlow!2026');const[busy,setBusy]=useState(false);const[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await fetch(`${API}/api/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const b=await r.json();if(!r.ok)throw new Error(b.error||'Login failed');sessionStorage.setItem('homeflow_token',b.token);sessionStorage.setItem('homeflow_user',JSON.stringify(b.user));onLogin(b.token,b.user)}catch(e:any){setError(e.message||'Login failed')}finally{setBusy(false)}}return <div className="login-page"><div className="login-glow"></div><section className="login-card"><img src="/pyrneo-logo.png" className="login-logo"/><p className="eyebrow">HOMEFLOW AI BY PYRNEO</p><h1>Human Settlements Delivery Assurance</h1><p className="login-copy">Secure access to the HomeFlow Command Centre, delivery intelligence and recovery workflows.</p><form onSubmit={submit}><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email"/></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password"/></label>{error&&<div className="login-error">{error}</div>}<button className="login-submit" disabled={busy}><LockKeyhole size={18}/>{busy?'Signing in…':'Sign in'}</button></form><div className="demo-accounts"><b>Hackathon demo access</b><span>Password for all demo accounts: <code>HomeFlow!2026</code></span>{demoAccounts.map(([e,r])=><button key={e} onClick={()=>setEmail(e)}><span>{r}</span><small>{e}</small></button>)}</div><small className="demo-note">Synthetic demonstration data only. Production identity is designed for Microsoft Entra ID.</small></section></div>}
function App(){const[token,setToken]=useState(()=>sessionStorage.getItem('homeflow_token')||'');const[user,setUser]=useState<User|null>(()=>{try{return JSON.parse(sessionStorage.getItem('homeflow_user')||'null')}catch{return null}});const[projects,setProjects]=useState<Project[]>([]);const[dashboard,setDashboard]=useState<Dashboard|null>(null);const[selected,setSelected]=useState<Project|null>(null);const[view,setView]=useState<View>('command');const[query,setQuery]=useState('');const[viewData,setViewData]=useState<any[]>([]);const[viewLoading,setViewLoading]=useState(false);const[drawer,setDrawer]=useState<any>(null);const[settingsOpen,setSettingsOpen]=useState(false);const[chatOpen,setChatOpen]=useState(false);const[messages,setMessages]=useState<ChatMsg[]>([{role:'assistant',text:'Hello. I am your HomeFlow delivery copilot. Ask what requires attention, open a province, review a contractor, or discuss any visible project.'}]);const[chatInput,setChatInput]=useState('');const[busy,setBusy]=useState(false);const[listening,setListening]=useState(false);const[provider,setProvider]=useState('rules');const[apiKey,setApiKey]=useState(()=>sessionStorage.getItem('homeflow_openai_key')||'');const[model,setModel]=useState<Model>(()=>(localStorage.getItem('homeflow_model')as Model)||'gpt-5.6-luna');const[voiceLang,setVoiceLang]=useState(()=>localStorage.getItem('homeflow_voice_lang')||'en-ZA');const[autoSpeak,setAutoSpeak]=useState(()=>localStorage.getItem('homeflow_auto_speak')!=='false');const[connection,setConnection]=useState('untested');const[connectionMessage,setConnectionMessage]=useState('');const audioRef=useRef<HTMLAudioElement|null>(null);const allowed=roleViews[user?.role||'']||[];const authHeaders=(json=true)=>{const h:Record<string,string>={Authorization:`Bearer ${token}`};if(json)h['Content-Type']='application/json';return h};const adminHeaders=()=>{const h=authHeaders();if(user?.role==='Administrator'&&apiKey)h['x-openai-api-key']=apiKey;return h};async function fetchJson(url:string,init?:RequestInit){const r=await fetch(url,{...init,headers:{...authHeaders(!init||init.method!=='GET'),...(init?.headers||{})}});if(r.status===401){logout(false);throw new Error('Session expired')}const b=await r.json();if(!r.ok)throw new Error(b.error||String(r.status));return b}function login(t:string,u:User){setToken(t);setUser(u)}function logout(call=true){if(call&&token)fetch(`${API}/api/auth/logout`,{method:'POST',headers:authHeaders()}).catch(()=>{});sessionStorage.removeItem('homeflow_token');sessionStorage.removeItem('homeflow_user');sessionStorage.removeItem('homeflow_openai_key');setToken('');setUser(null);setProjects([]);setDashboard(null)}async function logEvent(action:string,target:string){if(!token)return;fetch(`${API}/api/usage/events`,{method:'POST',headers:authHeaders(),body:JSON.stringify({action,target})}).catch(()=>{})}function navigate(v:View){if(!allowed.includes(v))return;setView(v);setDrawer(null);logEvent('Opened page',navItems.find(n=>n[0]===v)?.[1]||v)}useEffect(()=>{if(!token||!user)return;Promise.all([fetchJson(`${API}/api/projects`,{method:'GET'}),fetchJson(`${API}/api/dashboard`,{method:'GET'})]).then(([p,d])=>{setProjects(p);setDashboard(d);setSelected(p[0]||null)}).catch(()=>{})},[token,user?.id]);useEffect(()=>{if(!token||!['contractors','risks','recovery','audit','usage'].includes(view))return;setViewLoading(true);const ep=view==='usage'?'usage-logs':view==='recovery'?'recovery':view;fetchJson(`${API}/api/${ep}`,{method:'GET'}).then(setViewData).catch(()=>setViewData([])).finally(()=>setViewLoading(false))},[view,token]);const filtered=useMemo(()=>projects.filter(p=>`${p.name} ${p.id} ${p.province} ${p.municipality} ${p.contractor}`.toLowerCase().includes(query.toLowerCase())),[projects,query]);if(!token||!user)return <Login onLogin={login}/>;function stopVoice(){if(audioRef.current){audioRef.current.pause();audioRef.current.src='';audioRef.current=null}if('speechSynthesis'in window)speechSynthesis.cancel()}async function speakText(text:string){if(!autoSpeak||!text)return;stopVoice();try{const r=await fetch(`${API}/api/voice/speak`,{method:'POST',headers:adminHeaders(),body:JSON.stringify({text,language:voiceLang})});if(r.ok){const blob=await r.blob();const url=URL.createObjectURL(blob);const a=new Audio(url);audioRef.current=a;a.onended=()=>{URL.revokeObjectURL(url);audioRef.current=null};await a.play();return}}catch{}if(!('speechSynthesis'in window))return;const u=new SpeechSynthesisUtterance(text.replace(/[*#`]/g,''));u.lang=voiceLang;u.rate=.96;u.pitch=1;const voices=speechSynthesis.getVoices();u.voice=voices.find(v=>v.lang.toLowerCase()===voiceLang.toLowerCase())||voices.find(v=>v.lang.toLowerCase().startsWith(voiceLang.split('-')[0].toLowerCase()))||null;speechSynthesis.speak(u)}async function ask(text:string,voice=false){const q=text.trim();if(!q)return;setChatOpen(true);setMessages(m=>[...m,{role:'user',text:q}]);setBusy(true);try{const r=await fetch(`${API}/api/ai/copilot`,{method:'POST',headers:adminHeaders(),body:JSON.stringify({question:q,projectId:selected?.id,model})});const b=await r.json();const next=b.answer||b.error||'No grounded answer is available.';setProvider(b.provider||'rules');setMessages(m=>[...m,{role:'assistant',text:next}]);if(voice||autoSpeak)speakText(next)}catch{const next='I could not reach the HomeFlow AI service. Please try again.';setMessages(m=>[...m,{role:'assistant',text:next}])}finally{setBusy(false)}}async function loadBrief(){try{const b=await fetchJson(`${API}/api/executive-brief`,{method:'GET'});setDrawer({type:'brief',title:'Executive Brief',data:b})}catch(e:any){setDrawer({type:'message',title:'Executive Brief',data:{message:e.message}})}}async function openProvince(name:string){try{const d=await fetchJson(`${API}/api/provinces/${encodeURIComponent(name)}`,{method:'GET'});setDrawer({type:'province',title:name,data:d})}catch(e:any){setDrawer({type:'message',title:name,data:{message:e.message}})}}async function openContractor(name:string){try{const d=await fetchJson(`${API}/api/contractors/${encodeURIComponent(name)}`,{method:'GET'});setDrawer({type:'contractor',title:name,data:d})}catch(e:any){setDrawer({type:'message',title:name,data:{message:e.message}})}}function openProject(p:Project){setSelected(p);setDrawer({type:'project',title:p.name,data:p});logEvent('Opened project detail',p.name)}function startVoice(){const SR=(window as any).SpeechRecognition||(window as any).webkitSpeechRecognition;if(!SR){setChatOpen(true);setMessages(m=>[...m,{role:'assistant',text:'Voice recognition is not available in this browser. Chrome or Edge generally provide the broadest Web Speech support.'}]);return}stopVoice();const r=new SR();r.lang=voiceLang;r.interimResults=false;r.continuous=false;r.onstart=()=>setListening(true);r.onend=()=>setListening(false);r.onerror=()=>setListening(false);r.onresult=(e:any)=>handleCommand(e.results[0][0].transcript,true);r.start()}async function handleCommand(text:string,fromVoice=false){const t=text.trim();const q=t.toLowerCase();const nav:Array<[RegExp,View]>=[[/^(open|show|go to) (the )?(command centre|dashboard|home)$/i,'command'],[/^(open|show|go to) (the )?projects?$/i,'projects'],[/^(open|show|go to) (the )?(delivery )?map$/i,'map'],[/^(open|show|go to) (the )?contractors?$/i,'contractors'],[/^(open|show|go to) (the )?finance$/i,'finance'],[/^(open|show|go to) (the )?(risks|risks and blockers|blockers)$/i,'risks'],[/^(open|show|go to) (the )?(recovery|recovery plans?)$/i,'recovery'],[/^(open|show|go to) (the )?(usage|usage logs?)$/i,'usage']];for(const[n,v]of nav)if(n.test(t)&&allowed.includes(v)){navigate(v);const a=`Opening ${navItems.find(x=>x[0]===v)?.[1]}.`;setChatOpen(true);setMessages(m=>[...m,{role:'user',text:t},{role:'assistant',text:a}]);if(fromVoice)speakText(a);return}if(/^(executive brief|generate executive brief|read executive brief)$/i.test(t)){await loadBrief();return}if(/^(stop speaking|stop voice|be quiet)$/i.test(t)){stopVoice();return}const province=['Gauteng','KwaZulu-Natal','Limpopo','Eastern Cape','Free State','Western Cape','Mpumalanga','North West','Northern Cape'].find(x=>q.includes(x.toLowerCase()));if(province&&(q.includes('province')||q.includes('show')||q.includes('open'))){navigate('map');await openProvince(province);return}const id=t.match(/HF-\d+/i)?.[0];const byId=id?projects.find(p=>p.id.toLowerCase()===id.toLowerCase()):null;const byName=projects.find(p=>q.includes(p.name.toLowerCase().slice(0,Math.min(18,p.name.length))));if(byId||byName){openProject((byId||byName)!);return}await ask(t,fromVoice)}async function testOpenAI(){if(user.role!=='Administrator')return;if(!apiKey){setConnection('error');setConnectionMessage('Enter an OpenAI API key first.');return}setConnection('testing');setConnectionMessage('Testing connection…');try{const r=await fetch(`${API}/api/openai/test`,{method:'POST',headers:adminHeaders(),body:JSON.stringify({model})});const b=await r.json();if(!r.ok)throw new Error(b.error||'Connection failed');setConnection('ok');setConnectionMessage(b.message||'Connection successful.')}catch(e:any){setConnection('error');setConnectionMessage(e.message||'Connection failed')}}return <div className="app-shell"><aside className="sidebar"><div className="brand"><img src="/pyrneo-logo.png"/><div><b>HomeFlow AI</b><span>by Pyrneo</span></div></div><nav>{navItems.filter(([id])=>allowed.includes(id)).map(([id,label,Icon])=><button key={id} className={view===id?'active':''} onClick={()=>navigate(id)}><Icon/>{label}</button>)}</nav>{user.role==='Administrator'&&<button className="settings-nav" onClick={()=>setSettingsOpen(true)}><Settings size={17}/> AI & Voice Settings</button>}<div className="sidebar-user"><div><b>{user.name}</b><span>{user.role}</span></div><button onClick={()=>logout()} title="Sign out"><LogOut size={17}/></button></div><div className="sidebar-note"><Sparkles size={16}/><div><b>Responsible AI</b><span>Recommendations require accountable human review.</span></div></div></aside><main className="main"><header><div><p className="eyebrow">{view==='command'?'HOMEFLOW COMMAND CENTRE':view.toUpperCase()}</p><h1>{view==='command'?'Human Settlements Delivery Assurance':navItems.find(n=>n[0]===view)?.[1]}</h1></div><div className="header-actions"><label className="lang-select"><Languages size={15}/><select value={voiceLang} onChange={e=>{setVoiceLang(e.target.value);localStorage.setItem('homeflow_voice_lang',e.target.value)}}>{voiceLanguages.map(([v,l])=><option value={v} key={v}>{l}</option>)}</select></label>{user.role==='Administrator'&&<button className="ghost" onClick={()=>setSettingsOpen(true)}><KeyRound size={16}/> OpenAI</button>}<button className="ghost" onClick={loadBrief}><Sparkles size={17}/> Executive brief</button><button className={`voice ${listening?'live':''}`} onClick={startVoice}><Mic size={18}/>{listening?'Listening…':'Voice Agent'}</button></div></header>{view==='command'&&<CommandCentre dashboard={dashboard} projects={filtered} selected={selected} setSelected={(p:Project)=>{setSelected(p);openProject(p)}} query={query} setQuery={setQuery} ask={(q:string)=>handleCommand(q,false)} openProject={openProject}/>} {view==='projects'&&<ProjectsView projects={filtered} query={query} setQuery={setQuery} openProject={openProject}/>} {view==='map'&&<MapView projects={projects} openProvince={openProvince}/>} {view==='finance'&&<FinanceView projects={projects} openProject={openProject}/>} {view==='contractors'&&<ContractorsView data={viewData} loading={viewLoading} openContractor={openContractor}/>} {view==='risks'&&<RisksView data={viewData} loading={viewLoading} projects={projects} openProject={openProject}/>} {view==='recovery'&&<RecoveryView data={viewData} loading={viewLoading} projects={projects} openProject={openProject}/>} {view==='audit'&&<AuditView data={viewData} loading={viewLoading}/>} {view==='usage'&&<UsageView data={viewData} loading={viewLoading}/>}<footer><span>HomeFlow AI by Pyrneo</span><span>From stalled projects to completed homes.</span><span>{user.role} · Synthetic demo data</span></footer></main><button className="chat-fab" onClick={()=>setChatOpen(true)}><Bot size={23}/><span>Ask HomeFlow</span></button>{chatOpen&&<ChatPanel messages={messages} input={chatInput} setInput={setChatInput} busy={busy} provider={provider} onSend={()=>{const x=chatInput;setChatInput('');handleCommand(x,false)}} onVoice={startVoice} onClose={()=>setChatOpen(false)} onStop={stopVoice}/>} {drawer&&<DetailDrawer drawer={drawer} onClose={()=>setDrawer(null)} openProject={openProject}/>} {settingsOpen&&user.role==='Administrator'&&<SettingsPanel apiKey={apiKey} setApiKey={(v:string)=>{setApiKey(v);v?sessionStorage.setItem('homeflow_openai_key',v):sessionStorage.removeItem('homeflow_openai_key');setConnection('untested')}} model={model} setModel={(v:Model)=>{setModel(v);localStorage.setItem('homeflow_model',v)}} autoSpeak={autoSpeak} setAutoSpeak={(v:boolean)=>{setAutoSpeak(v);localStorage.setItem('homeflow_auto_speak',String(v));if(!v)stopVoice()}} connection={connection} message={connectionMessage} test={testOpenAI} close={()=>setSettingsOpen(false)}/>}</div>}
function CommandCentre({dashboard,projects,selected,setSelected,query,setQuery,ask,openProject}:any){return <><section className="hero"><div><b><span className="pulse-dot"></span>Delivery intelligence is active</b><p>Monitor → Anticipate → Explain → Act → Escalate → Verify → Learn</p></div><button onClick={()=>ask('What requires my attention today?')}><Sparkles size={16}/> What requires attention today?</button></section><section className="metrics"><Metric label="Projects" value={dashboard?.totalProjects??'—'} icon={Building2}/><Metric label="Critical" value={dashboard?.critical??'—'} icon={AlertTriangle} tone="red"/><Metric label="At Risk" value={dashboard?.atRisk??'—'} icon={Activity} tone="amber"/><Metric label="Units completed" value={(dashboard?.unitsCompleted??0).toLocaleString()} icon={Home} tone="green"/><Metric label="Overdue milestones" value={dashboard?.overdueMilestones??'—'} icon={FileCheck2} tone="amber"/><Metric label="Open risks" value={dashboard?.openRisks??'—'} icon={ShieldCheck} tone="red"/></section><section className="workspace"><Portfolio projects={projects} selected={selected} setSelected={setSelected} query={query} setQuery={setQuery}/><div className="insight card"><div className="card-head"><div><h2>Priority project intelligence</h2><p>Click any project for a full detailed view.</p></div><Gauge/></div>{selected?<ProjectInsight p={selected} ask={ask} openProject={openProject}/>:<div className="empty-state">Select a project.</div>}</div></section></>}
function Portfolio({projects,selected,setSelected,query,setQuery}:any){return <section className="card"><div className="card-head"><div><h2>Portfolio priorities</h2><p>Analysis prioritises project names and delivery health.</p></div><div className="search"><Search size={15}/><input placeholder="Search project, province or contractor…" value={query} onChange={e=>setQuery(e.target.value)}/></div></div><div className="project-list">{[...projects].sort((a,b)=>a.healthScore-b.healthScore).map((p:any)=><button className={`project-row ${selected?.id===p.id?'selected':''}`} key={p.id} onClick={()=>setSelected(p)}><span className={`score ${statusClass(p.status)}`}>{p.healthScore}</span><span className="project-main"><b>{p.name}</b><span>{p.province} · {p.municipality} · {p.contractor}</span></span><span className="mini"><span>Physical</span><b>{p.physicalProgress}%</b></span><span className="mini"><span>Spend</span><b>{Math.round(p.expenditure/p.budget*100)}%</b></span><Status value={p.status}/><ChevronRight size={16}/></button>)}</div></section>}
function ProjectInsight({p,ask,openProject}:{p:Project;ask:(q:string)=>void;openProject:(p:Project)=>void}){const spend=Math.round(p.expenditure/p.budget*100),gap=spend-p.physicalProgress;return <div className="project-insight"><div className="health"><div className={`health-ring ${statusClass(p.status)}`}><strong>{p.healthScore}</strong><span>/100</span></div><div><b>{p.name}</b><span>{p.status} · {Math.round(p.confidence*100)}% confidence · {p.trend}</span></div></div><div className="bars"><label>Budget expenditure <b>{spend}%</b></label><div><i style={{width:`${spend}%`}}></i></div><label>Physical progress <b>{p.physicalProgress}%</b></label><div><i className="physical" style={{width:`${p.physicalProgress}%`}}></i></div></div><div className={`divergence ${gap>15?'danger':''}`}><BarChart3 size={18}/><div><b>{gap} pt divergence</b><span>{currency(p.expenditure)} spent of {currency(p.budget)}</span></div></div><div className="facts"><div><span>Overdue milestones</span><b>{p.overdueMilestones}</b></div><div><span>Open risks</span><b>{p.openRisks}</b></div><div><span>Evidence age</span><b>{p.evidenceAgeDays}d</b></div><div><span>Units completed</span><b>{p.unitsCompleted}/{p.unitsPlanned}</b></div></div><div className="blocker"><span>Primary blocker</span><b>{p.primaryBlocker}</b></div><div className="insight-actions three"><button onClick={()=>ask(`Why is ${p.name} at risk?`)}>Why this score?</button><button onClick={()=>ask(`What should we do next for ${p.name}?`)}>Recommended action</button><button onClick={()=>openProject(p)}>Full project view</button></div></div>}
function ProjectsView({projects,query,setQuery,openProject}:any){return <section className="card full-view"><div className="card-head"><div><h2>Human Settlements Projects</h2><p>{projects.length} projects visible to your role. Click any row for the full project workspace.</p></div><div className="search wide"><Search size={16}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search projects…"/></div></div><div className="data-table project-table"><div className="table-row header"><span>Project</span><span>Province</span><span>Contractor</span><span>Health</span><span>Status</span></div>{projects.map((p:Project)=><button className="table-row clickable" key={p.id} onClick={()=>openProject(p)}><span><b>{p.name}</b><small>{p.id} · {p.programme}</small></span><span>{p.province}</span><span>{p.contractor}</span><span>{p.healthScore}/100</span><span><Status value={p.status}/></span></button>)}</div></section>}
const provincePoints:Record<string,[number,number]>={'Western Cape':[185,415],'Northern Cape':[265,275],'Eastern Cape':[400,410],'Free State':[385,300],'North West':[350,220],'Gauteng':[430,205],'Limpopo':[475,120],'Mpumalanga':[515,220],'KwaZulu-Natal':[525,345]};
function MapView({projects,openProvince}:any){const provinces=[...new Set(projects.map((p:Project)=>p.province))];return <section className="map-layout"><div className="card map-card"><div className="card-head"><div><h2>South Africa Delivery Map</h2><p>Interactive portfolio map. Select a province for its full delivery status.</p></div><MapPinned/></div><div className="sa-svg-wrap"><svg viewBox="0 0 650 520" className="sa-svg"><path className="country" d="M112 170 L170 118 L252 82 L356 72 L449 88 L526 128 L580 194 L590 284 L554 365 L496 427 L410 463 L310 468 L214 440 L143 385 L101 306 L92 232 Z"/><path className="coast" d="M112 170 L170 118 L252 82 L356 72 L449 88 L526 128 L580 194 L590 284 L554 365 L496 427 L410 463 L310 468 L214 440 L143 385 L101 306 L92 232 Z"/><circle className="lesotho" cx="455" cy="365" r="24"/><text x="455" y="369" textAnchor="middle" className="lesotho-label">LS</text>{provinces.map((prov:string)=>{const[x,y]=provincePoints[prov]||[320,260];const ps=projects.filter((p:Project)=>p.province===prov);const worst=[...ps].sort((a,b)=>a.healthScore-b.healthScore)[0];return <g key={prov} className={`province-node ${statusClass(worst?.status||'Healthy')}`} onClick={()=>openProvince(prov)}><circle cx={x} cy={y} r="28"/><text x={x} y={y-2} textAnchor="middle" className="pin-count">{ps.length}</text><text x={x} y={y+44} textAnchor="middle" className="pin-label">{prov}</text></g>})}</svg></div></div><div className="card map-side"><div className="card-head"><div><h2>Provincial delivery status</h2><p>Click a province for full detail.</p></div></div>{provinces.map((prov:string)=>{const ps=projects.filter((p:Project)=>p.province===prov),score=Math.round(ps.reduce((s:number,p:Project)=>s+p.healthScore,0)/ps.length),critical=ps.filter((p:Project)=>p.status==='Critical').length;return <button className="region-row" key={prov} onClick={()=>openProvince(prov)}><span><b>{prov}</b><small>{ps.length} projects · {critical} critical</small></span><strong>{score}/100</strong></button>})}</div></section>}
function FinanceView({projects,openProject}:any){return <section className="card full-view"><div className="card-head"><div><h2>Budget vs Physical Progress</h2><p>Every row opens the complete project view.</p></div><CircleDollarSign/></div><div className="data-table"><div className="table-row header"><span>Project</span><span>Budget</span><span>Spend</span><span>Physical</span><span>Divergence</span></div>{[...projects].sort((a,b)=>(b.expenditure/b.budget*100-b.physicalProgress)-(a.expenditure/a.budget*100-a.physicalProgress)).map((p:Project)=>{const spend=Math.round(p.expenditure/p.budget*100),d=spend-p.physicalProgress;return <button className="table-row clickable" key={p.id} onClick={()=>openProject(p)}><span><b>{p.name}</b><small>{p.province}</small></span><span>{currency(p.budget)}</span><span>{spend}%</span><span>{p.physicalProgress}%</span><span className={d>15?'danger-text':''}>{d} pts</span></button>})}</div></section>}
function ContractorsView({data,loading,openContractor}:any){if(loading)return <Loading/>;return <section className="card full-view"><div className="card-head"><div><h2>Contractor Performance</h2><p>Click a contractor to see every assigned project and status.</p></div><Users/></div><div className="data-cards">{data.map((x:any)=><button className="data-card clickable-card" key={x.name} onClick={()=>openContractor(x.name)}><div className="data-card-top"><b>{x.name}</b><ChevronRight/></div><p>{x.projects} projects · average health {x.averageHealth}/100</p><small>{x.criticalProjects} critical · {x.atRiskProjects} at risk · {x.overdueMilestones} overdue milestones</small><em>{currency(x.contractValue||0)} portfolio value</em></button>)}</div></section>}
function RisksView({data,loading,projects,openProject}:any){if(loading)return <Loading/>;return <section className="card full-view"><div className="card-head"><div><h2>Risks & Blockers</h2><p>Click any risk card to open its project detail.</p></div><AlertTriangle/></div><div className="data-cards">{data.map((x:any)=><button className="data-card clickable-card" key={x.projectId} onClick={()=>{const p=projects.find((p:Project)=>p.id===x.projectId);if(p)openProject(p)}}><div className="data-card-top"><b>{x.projectName}</b><Status value={x.status}/></div><p>{x.primaryBlocker}</p><small>{x.openRisks} open risks · evidence {x.evidenceAgeDays} days old</small><em>{x.financialPhysicalDivergence} pt budget/progress divergence</em></button>)}</div></section>}
function RecoveryView({data,loading,projects,openProject}:any){if(loading)return <Loading/>;return <section className="card full-view"><div className="card-head"><div><h2>Recovery Plans</h2><p>Corrective actions, owners and delivery deadlines.</p></div><Activity/></div><div className="data-cards">{data.map((x:any)=><button className="data-card clickable-card" key={x.id} onClick={()=>{const p=projects.find((p:Project)=>p.id===x.projectId);if(p)openProject(p)}}><div className="data-card-top"><b>{x.title}</b><Status value={x.projectStatus}/></div><p>{x.projectName}</p><small>{x.owner} · due {x.due}</small><em>{x.status}</em></button>)}</div></section>}
function AuditView({data,loading}:any){if(loading)return <Loading/>;return <section className="card full-view"><div className="card-head"><div><h2>Audit & Governance</h2><p>Material project and AI-assisted actions.</p></div><ShieldCheck/></div><div className="audit-list">{data.map((x:any,i:number)=><div className="audit-row" key={i}><div><b>{x.action}</b><span>{x.record}</span></div><div><b>{x.user}</b><span>{x.role}</span></div><div><b>{new Date(x.at).toLocaleString()}</b><span>{x.aiAssisted?'AI-assisted':'Human action'}</span></div></div>)}</div></section>}
function UsageView({data,loading}:any){if(loading)return <Loading/>;return <section className="card full-view"><div className="card-head"><div><h2>Usage Logs</h2><p>Who used HomeFlow, when, and which actions they performed.</p></div><ClipboardList/></div><div className="audit-list">{data.map((x:any,i:number)=><div className="audit-row" key={i}><div><b>{x.user}</b><span>{x.email}</span></div><div><b>{x.action}</b><span>{x.target}</span></div><div><b>{x.role}</b><span>{new Date(x.at).toLocaleString()}</span></div></div>)}</div></section>}
function Loading(){return <section className="card full-view loading-view">Loading HomeFlow data…</section>}
function ChatPanel({messages,input,setInput,busy,provider,onSend,onVoice,onClose,onStop}:any){return <aside className="copilot"><div className="copilot-head"><div><Bot/><div><b>HomeFlow Voice Copilot</b><span>Conversational · grounded · {provider==='openai'?'OpenAI':'rules fallback'}</span></div></div><button onClick={onClose}><X/></button></div><div className="chat conversation">{messages.map((m:ChatMsg,i:number)=><div key={i} className={`bubble ${m.role}`}>{m.text}</div>)}{busy&&<div className="bubble assistant typing">•••</div>}</div><div className="quick-prompts"><button onClick={()=>setInput('What requires my attention today?')}>Today's priorities</button><button onClick={()=>setInput('Which projects have the largest budget-to-progress gap?')}>Budget gaps</button><button onClick={()=>setInput('Which contractors need management review?')}>Contractors</button></div><form onSubmit={(e)=>{e.preventDefault();onSend()}}><button type="button" className="mic-only" onClick={onVoice}><Mic size={17}/></button><input value={input} onChange={e=>setInput(e.target.value)} placeholder="Ask HomeFlow anything…"/><button type="submit">Send</button><button type="button" className="mic-only" onClick={onStop}><VolumeX size={17}/></button></form></aside>}
function DetailDrawer({drawer,onClose,openProject}:any){const d=drawer.data;return <div className="drawer-backdrop" onClick={onClose}><aside className="detail-drawer" onClick={e=>e.stopPropagation()}><div className="drawer-head"><div><p className="eyebrow">DETAILED VIEW</p><h2>{drawer.title}</h2></div><button onClick={onClose}><X/></button></div>{drawer.type==='project'&&<ProjectDetail p={d}/>} {drawer.type==='province'&&<ProvinceDetail d={d} openProject={openProject}/>} {drawer.type==='contractor'&&<ContractorDetail d={d} openProject={openProject}/>} {drawer.type==='brief'&&<BriefDetail d={d}/>} {drawer.type==='message'&&<div className="drawer-body"><p>{d.message}</p></div>}</aside></div>}
function ProjectDetail({p}:{p:Project}){const spend=Math.round(p.expenditure/p.budget*100),gap=spend-p.physicalProgress;return <div className="drawer-body"><div className="drawer-status"><Status value={p.status}/><span>Health {p.healthScore}/100</span><span>{p.province}</span></div><div className="detail-grid"><Info label="Project ID" value={p.id}/><Info label="Programme" value={p.programme}/><Info label="Municipality" value={p.municipality}/><Info label="Ward" value={p.ward}/><Info label="Stage" value={p.stage}/><Info label="Project manager" value={p.projectManager}/><Info label="Implementing agent" value={p.implementingAgent}/><Info label="Contractor" value={p.contractor}/><Info label="Start date" value={p.startDate}/><Info label="Forecast completion" value={p.forecastCompletion}/></div><h3>Delivery performance</h3><div className="detail-kpis"><Info label="Budget" value={currency(p.budget)}/><Info label="Expenditure" value={`${currency(p.expenditure)} (${spend}%)`}/><Info label="Physical progress" value={`${p.physicalProgress}%`}/><Info label="Planned progress" value={`${p.plannedProgress}%`}/><Info label="Budget/progress gap" value={`${gap} pts`}/><Info label="Units completed" value={`${p.unitsCompleted}/${p.unitsPlanned}`}/></div><h3>Risk & evidence</h3><div className="callout danger-callout"><b>{p.primaryBlocker}</b><span>{p.openRisks} open risks · {p.overdueMilestones} overdue milestones · evidence {p.evidenceAgeDays} days old</span></div>{p.rootCauses.length>0&&<ul>{p.rootCauses.map(x=><li key={x}>{x}</li>)}</ul>}<h3>Recovery actions</h3>{p.recoveryActions.length?p.recoveryActions.map(a=><div className="action-row" key={a.id}><div><b>{a.title}</b><span>{a.owner}</span></div><div><b>{a.status}</b><span>Due {a.due}</span></div></div>):<p className="muted">No active recovery plan is required.</p>}<h3>Recent activity</h3>{p.activity.map((a,i)=><div className="action-row" key={i}><div><b>{a.action}</b><span>{a.actor}</span></div><span>{new Date(a.at).toLocaleString()}</span></div>)}</div>}
function ProvinceDetail({d,openProject}:any){return <div className="drawer-body"><div className="detail-kpis"><Info label="Projects" value={d.summary.projects}/><Info label="Average health" value={`${d.summary.averageHealth}/100`}/><Info label="Critical" value={d.summary.critical}/><Info label="At risk" value={d.summary.atRisk}/><Info label="Units completed" value={`${d.summary.unitsCompleted}/${d.summary.unitsPlanned}`}/><Info label="Budget" value={currency(d.summary.budget)}/></div><h3>Projects in {d.name}</h3>{d.projects.map((p:Project)=><button className="drawer-project" key={p.id} onClick={()=>openProject(p)}><div><b>{p.name}</b><span>{p.municipality} · {p.contractor}</span></div><div><strong>{p.healthScore}/100</strong><Status value={p.status}/></div></button>)}</div>}
function ContractorDetail({d,openProject}:any){return <div className="drawer-body"><div className="detail-kpis"><Info label="Projects" value={d.summary.projects}/><Info label="Average health" value={`${d.summary.averageHealth}/100`}/><Info label="Critical" value={d.summary.critical}/><Info label="At risk" value={d.summary.atRisk}/><Info label="Units completed" value={d.summary.unitsCompleted}/><Info label="Portfolio value" value={currency(d.summary.contractValue)}/></div><h3>Assigned projects</h3>{d.projects.map((p:Project)=><button className="drawer-project" key={p.id} onClick={()=>openProject(p)}><div><b>{p.name}</b><span>{p.province} · {p.municipality}</span></div><div><strong>{p.healthScore}/100</strong><Status value={p.status}/></div></button>)}</div>}
function BriefDetail({d}:any){return <div className="drawer-body"><div className="brief-lead">{d.summary}</div><h3>Priority projects</h3>{(d.priorities||[]).map((x:any)=><div className="priority-row" key={x.id}><div><b>{x.name}</b><span>{x.province} · {x.status}</span></div><strong>{x.score}/100</strong><p>{x.reason}</p></div>)}<h3>Decisions required</h3><ul>{(d.decisions||[]).map((x:string,i:number)=><li key={i}>{x}</li>)}</ul><p className="muted">{d.note}</p></div>}
function Info({label,value}:{label:string;value:any}){return <div className="info"><span>{label}</span><b>{value}</b></div>}
function SettingsPanel({apiKey,setApiKey,model,setModel,autoSpeak,setAutoSpeak,connection,message,test,close}:any){return <div className="drawer-backdrop" onClick={close}><aside className="settings-panel" onClick={e=>e.stopPropagation()}><div className="drawer-head"><div><p className="eyebrow">ADMINISTRATOR ONLY</p><h2>AI & Voice Settings</h2></div><button onClick={close}><X/></button></div><div className="drawer-body"><p className="muted">OpenAI configuration is intentionally restricted to the Administrator role. Keys are retained only for this browser session in hackathon mode.</p><label className="field">OpenAI API key<input type="password" value={apiKey} onChange={e=>setApiKey(e.target.value)} placeholder="sk-…"/></label><label className="field">AI model<select value={model} onChange={e=>setModel(e.target.value)}><option value="gpt-5.6-luna">GPT-5.6 Luna</option><option value="gpt-5.6-terra">GPT-5.6 Terra</option><option value="gpt-5.6-sol">GPT-5.6 Sol</option></select></label><label className="toggle"><input type="checkbox" checked={autoSpeak} onChange={e=>setAutoSpeak(e.target.checked)}/><span>Automatically play humanlike spoken responses</span></label><button className="primary" onClick={test}><KeyRound size={16}/> Test OpenAI connection</button><div className={`connection ${connection}`}>{message||'Connection not tested.'}</div><div className="security-note"><ShieldCheck/><span>For production, move the OpenAI credential to Azure Key Vault or a server-side Render secret. Do not commit API keys to GitHub.</span></div></div></aside></div>}
createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);
